# Web

Current state: static scaffold.

Next steps (typical):
- Add a UI for filtering by Zachman `row` and `column`.
- Add an API layer (or local querying approach) to read ingested data from the database.

